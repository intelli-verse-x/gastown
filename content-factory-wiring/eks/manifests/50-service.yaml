apiVersion: v1
kind: Service
metadata:
  name: contentx-postiz-webhook
  namespace: content-factory
  labels:
    app.kubernetes.io/name: contentx-postiz-webhook
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: contentx-postiz-webhook
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
